experiment_name: "vis_quick_test_vit_b_a40"
output_base_dir: "runs"
augmentation: null
training:
  images_dir: "dat/day_8_aug/images"
  labels_dir: "dat/day_8_aug/labels"
  model_type: "vit_b_lm"
  patch_shape: [2048, 2048]  # Full image size - no cropping/patching
  batch_size: 4  # Increased from 1 - plenty of GPU memory available (only using 13GB/80GB)
  n_samples: null  # null = use actual dataset size (all images, no random sampling)
  n_epochs: 3
  num_workers: 8
  learning_rate: 1.0e-5
  val_split: 0.05
  shuffle_data: true
  shuffle_seed: 42
  train_instance_segmentation_only: false
  use_min_instance_sampler: true
  min_instances_per_patch: 1
  min_instance_size: 1  # Minimum 1 pixel to avoid empty objects but keep all real instances
  checkpoint_name: "vis_quick_test_vit_b_a40"
  save_validation_predictions_frequency: 1
  resume_from_checkpoint: null