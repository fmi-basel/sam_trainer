experiment_name: "full_sam_vit_b_a100"
output_base_dir: "runs"
augmentation: null
training:
  images_dir: "dat/training_data/images"
  labels_dir: "dat/training_data/labels"
  model_type: "vit_b_lm"
  patch_shape: [512, 512]
  batch_size: 32
  n_epochs: 100
  n_samples: 300
  num_workers: 16
  learning_rate: 2.0e-4
  val_split: 0.1
  shuffle_data: true
  shuffle_seed: 42
  train_instance_segmentation_only: false
  use_min_instance_sampler: true
  min_instances_per_patch: 1
  min_instance_size: 25
  checkpoint_name: "full_sam_vit_b_a100"
  resume_from_checkpoint: null
  export_path: "final_models/full_sam_vit_b_a100.pt"
